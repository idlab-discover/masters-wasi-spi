[workspace]
resolver = "2"

members = [
  "compiler",
  "host",
  "lib/host/spi", 
  "lib/host/delay", 
  "lib/host/gpio",
  "guest"
, "linux-host"]

[profile.release]
opt-level = "z"
strip = true
debug = false
lto = true
codegen-units = 1

[profile.release.package.host]
strip = false
debug = 2   
