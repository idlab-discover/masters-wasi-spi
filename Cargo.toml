[workspace]
resolver = "2"

members = [
  "compiler",
  "host",
  "lib/spi", 
  "lib/delay", 
  "lib/gpio",
  "guest"
]

[profile.release]
opt-level = "z"
strip = true
debug = false
lto = true
codegen-units = 1

[profile.release.package.host]
strip = false
debug = 2   
