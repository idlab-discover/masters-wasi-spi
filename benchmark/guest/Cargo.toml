[package]
name = "benchmark-guest"
version = "0.1.0"
edition = "2024"

[dependencies]
wit-bindgen = "0.48.1"
pingpong = { path = "../pingpong" }
wasi-embedded-hal = { path = "../../lib/guest/wasi-embedded-hal" }

[lib]
crate-type = ["cdylib"]
